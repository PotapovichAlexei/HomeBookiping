#pragma once
#include "mainHeader.h"

class Date {
public:
    int _day;
    int _month;
    int _year;
public:
    Date(int day, int month, int year) : _day(day), _month(month), _year(year) {};

};

